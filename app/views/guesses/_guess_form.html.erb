<%= form_for([ @game, @game.guesses.build ]) do |f| %>
  <%= f.label(:letter, "Enter Guess:") %>
  <%= f.text_field :letter, pattern: /^[A-Za-z]$/, :class => "form-control", :maxlength => 1, :autofocus => true %>
  <%= f.submit("Submit") %>
<% end %>
